# pnpm workspace roots. The template ships no workspace packages yet; application
# and shared packages are added in later batches (the app profile roots them under
# portals/, per the governance source-directory slots). Keep this file present so
# the repo is a pnpm workspace from day one.
packages:
  - "portals/*"
  - "portals/packages/*"
